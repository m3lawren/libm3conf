Tokens:

id   ::= [a-zA-Z_][a-zA-Z0-9_.]*
str  ::= "([^\"]|\"|\\)*"
int  ::= (0|-?[1-9][0-9]*)
lb   ::= {
rb   ::= }
eq   ::= =
semi ::= ;
sec  ::= section
ws   ::= [ \t\r\n]*
com  ::= #[^\r\n]*[\r\n]

ws and com are not passed through to the parser.
